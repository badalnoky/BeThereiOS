import SwiftUI

struct SearchView: View {
    var body: some View {
        Text("Search")
    }
}

#if DEBUG
struct SearchView_Previews: PreviewProvider {
    static var previews: some View {
        SearchView()
    }
}
#endif
